# PCB 叠构生成与孔型绘制 — 代码说明文档

> 对应文件：`12.18日的叠构生成及测试.xlsm` 的 VBA 模块 **`模块1`**
> 说明：本工具在 Excel 内根据 `Config` 表参数自动生成 PCB 叠构（层结构）数据表，并在左侧以图形方式绘制盲孔 / 埋孔 / 通孔 / 背钻等孔型。

---

## 一、总体框架

整个工程为一个标准 VBA 模块，按功能划分为 7 个 Section（代码内以注释分隔）：

| Section | 职责 | 关键函数 |
|---|---|---|
| 0 通用清理 | 清空工作表图形 | `CleanAllShapes` |
| 1 通用工具 & 初始化 | 找/建表、读配置、初始化 Config 与 MaterialsIndex | `EnsureSheet`、`GetConfigValue`、`InitializeConfigSheetIfMissing`、`EnsureConfigExtraKeys` |
| 2 UDF 联动 | 根据材料表自动查 Vendor(mm)/DK/DF | `MatVendor`、`MatDK`、`MatDF` |
| 3 Config UI | 生成下拉选项、层对表 | `SetupConfigUI`、`BuildInterlayerPPTable`、`SetDV_List` |
| 4 Runner 入口 | 一键执行 | `RunIfReady`、`OneClick_Setup_And_Run` |
| 5 叠构生成核心 | 写表 + 绘制孔型 | `BuildStackup`、`GenerateStackupLayout`、`DrawXXX` 系列 |
| 6 层对映射 | Config 的层间选材回填到叠构表 | `ApplyInterlayerSelections`、`ReapplyUDFForAllDielectrics` |
| 7 一键清理 | 清形状/清删除线 | `QuickClean_Shapes_And_Strikethrough` |

**执行流程（`BuildStackup`）**
1. 确保 `Config` / `MaterialsIndex` / `Stack-up(Optimized)` 三个表存在。
2. 读取 `Config` 全部参数（层数、对称、混压、孔型等）。
3. `GenerateStackupLayout`：清空 → 写表头 → 逐层写 S/M / 铜层 / 介质层 → 写 Totals → 绘制孔型图形。
4. `ApplyInterlayerSelections`：把 Config 里逐层配的 PP 型号/含胶/ply 回填。
5. 样式 / 列宽 / 冻结 / 校验 / 清理残留图形。

---

## 二、列布局（本次调整后）

叠构数据表整体右移 **1 列**，最左侧 A 列专用于**孔型图形**（盲孔 / 埋孔 / 通孔 / 背钻共用同一列）。

| 列 | 常量 | 含义 |
|---|---|---|
| A | `COL_VIA = 1` | 孔型图形列（盲孔/埋孔/通孔/背钻同列绘制） |
| B | `COL_ID = 2` | 标识（L1、PP、CORE、S/M、Totals…） |
| C | `COL_TYPE = 3` | 层类别 |
| D | `COL_COPPER = 4` | 铜箔 |
| E | `COL_MAT_SHEET = 5` | 材料表 |
| F | `COL_CODE = 6` | PP Glass style |
| G | `COL_PARAM = 7` | Param(含胶)/Resin(%) |
| H | `COL_PLY = 8` | ply |
| I | `COL_RESIDUAL = 9` | 残铜率(%) |
| J | `COL_VENDOR = 10` | Vendor(mm) |
| K | `COL_DK = 11` | DK |
| L | `COL_DF = 12` | DF |
| M | `COL_ACTUAL = 13` | Actual(mm) |
| N | `COL_CUST_SPEC = 14` | 客规(um) |
| O | `COL_PLANT_SPEC = 15` | 厂规(um) |

**行号与层的映射（`RowIndexForLayer`）**

```
row 1  = 表头
row 2  = S/M (Top)
row 3  = L1      RowIndexForLayer(n) = 3 + (n-1)*2
row 4  = 介质(PP/CORE)   ← 位于 L1 与 L2 之间
row 5  = L2
...
row (2n+2) = S/M (Bottom)
row (2n+3) = Totals
```

---

## 三、孔型绘制规则（核心）

所有孔型在 `GenerateStackupLayout` 中按顺序绘制，均在 **A 列** 垂直方向叠画：

| 孔型 | 配置项 | 图形 | 颜色 / 尺寸 |
|---|---|---|---|
| 盲孔 | `Laser阶数` | **每 1 阶 = 1 个梯形，多阶上下堆叠** | 黑色，宽≈列宽 75%、下宽:上宽≈1.6 |
| 埋孔 | `埋孔`（如 `L2-L6`） | 圆柱（矩形） | 橙色，宽≈列宽 45% |
| 通孔 | `通孔`（默认 是） | **细圆柱（缩小）** L1~LBottom | 金色，宽≈列宽 28% |
| 背钻 | `背钻`（如 `L1-L3`） | 宽圆柱叠在通孔两端 | 灰色半透明，宽≈列宽 70% |

### 盲孔细节（三阶示例）

`Laser阶数 = 3` 表示“打 3 次激光”，即 3 个梯形堆叠：

- **顶侧**（自上而下，锥尖朝下）：
  `L1→L2`、`L2→L3`、`L3→L4`（共 3 个梯形，只给第一个加“盲 L1-L4”标签）
- **底侧**（自下而上，锥尖朝上，镜像对称）：
  `L8→L7`、`L7→L6`、`L6→L5`（共 3 个梯形）

实现函数：`DrawBlindVias`（按阶循环）→ `DrawBlindTrapezoid`（单梯形）→ `AddTrapezoidWedge`（Freeform 画梯形）。

### 绘制函数一览

| 函数 | 作用 |
|---|---|
| `DrawBlindVias` | 盲孔主入口，按“阶”循环画堆叠梯形（上下对称） |
| `DrawBlindTrapezoid` | 画单个盲孔梯形（可镜像，可省略标签） |
| `DrawBuriedVias` | 埋孔：每个 `L?–L?` 画一个圆柱 |
| `DrawThroughVias` | 通孔：L1~LBottom 一条细金圆柱 |
| `DrawBackDrillVias` | 背钻：在通孔两端叠加更宽的半透明灰圆柱 |
| `DrawCylinderVia` | 圆柱通用绘制（参数：`widthRatio` 宽度比例、`transparency` 透明度） |
| `AddTrapezoidWedge` | 用 `BuildFreeform` 构建梯形（自由形状） |
| `AddViaLabel` | 为自由形状加独立文字标签（避免自由形状直接加字产生多余 Line） |
| `LabelShape` | 一般形状的文字标签（TextFrame2 优先，回退 TextFrame） |
| `RemoveStrayShapes` | 删除非本程序命名（不以 `G_PREFIX` 开头）的残留图形 |
| `CleanAllShapes` | 逐删该表全部形状（避免边遍历边删遗漏） |

**颜色常量（在调用处传入）**
- 盲孔 黑 `RGB(0,0,0)`；埋孔 橙 `RGB(255,165,0)`；通孔 金 `RGB(255,215,0)`；背钻 灰 `RGB(150,150,150)`。

**几何常量（可调，位于常量区）**
- `COL_WIDTH_USAGE_RATIO=0.75` 盲孔底宽占列宽上限
- `TOP_TO_BOTTOM_RATIO=1.6` 盲孔上底:下底宽比
- `MIN_TOP_WIDTH=6` 盲孔上底最小宽(pt)

---

## 四、Config 配置项一览

| 配置项 | 默认 | 说明 |
|---|---|---|
| `Execute` | TRUE | TRUE=运行生成 |
| `总层数` | 8 | PCB 总层数 (1–50) |
| `CORE数量` | 1 | CORE 层数量 |
| `是否对称` | TRUE | 对称镜像 |
| `埋孔` | NA | 埋孔层对，如 `L2-L6;L3-L5` |
| `通孔` | 是 | 通孔；`否` 则不画 |
| `背钻` | NA | 背钻层对，如 `L1-L3;L8-L6` |
| `Laser阶数` | NA | 盲孔阶数；数字 N = N 个堆叠梯形 |
| `量测方式` | SM-SM | SM-SM / Au-Au / Cu-Cu |
| `目标总厚` | 1.2 | 目标总厚度(mm) |
| `使用油墨型号` | 4000 MEH | S/M 油墨名 |
| `是否混压` | 否 | 混压分材 |
| `材料1` / `材料2` | S1170G / NA | 材料表 |
| `残铜→厚度效应` | 2 | 残铜对厚度的效应 |
| `启用冻结保护` | TRUE | 是否冻结表头 |
| `纯压PP Code` / `混压时 PPCode` | 1056 / NA | PP 默认型号 |
| `Core 厚度` | 50.8 | CORE 厚度(um) |

> `通孔` 与 `背钻` 为新增项；旧 Config 表缺少时会由 `EnsureConfigExtraKeys` 自动补齐。

---

## 五、如何扩展新孔型

新增一种孔型只需三步（框架已解耦）：

1. **加 Config 项**：在 `InitializeConfigSheetIfMissing` 的 `configData` 数组和 `EnsureConfigExtraKeys` 中各加一行。
2. **加绘制函数**：仿照 `DrawThroughVias`，写一个 `DrawXxxVias(ws, n, spec)`，内部复用 `DrawCylinderVia`（圆柱类）或 `AddTrapezoidWedge`（锥形类）。
3. **挂到主流程**：在 `BuildStackup` 读取该配置，并在 `GenerateStackupLayout` 的“孔型绘制”段按顺序调用。

例如新增“盘中孔(PTH)”：`埋孔` 逻辑即可复用，只需换个颜色/宽度。

---

## 六、已知注意事项

1. **遗留细线（Line）**：叠构表在 `ApplyInterlayerSelections` 公式运算时会物化出两条极细的残留 `Line` 图形（原模板即存在，属视图层现象，不影响数据与孔型）。已通过 `RemoveStrayShapes` + `ResetWindowPanes` 兜底清理。
2. **自由形状不要直接加文字**：`BuildFreeform` 生成的自由形状若直接用 `TextFrame2` 加字会产生多余 `Line` 图形，故盲孔标签改用独立文本框 `AddViaLabel`。
3. **列引用一律用常量**：新增列时只改常量区 `COL_*`，样式/校验/清理函数已全部参数化，避免硬编码列字母。